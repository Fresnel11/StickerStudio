# Configuration de l'URL du backend API
# En local (vide = proxy Vite vers http://127.0.0.1:3001) :
# VITE_API_URL=
# En production (URL absolue du backend Render si hébergement frontend séparé) :
VITE_API_URL=https://stickerstudio.onrender.com
